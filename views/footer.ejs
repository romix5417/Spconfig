            <footer class="modal-footer">
                <a style="text-align: right; color: #1a1a1a; text-decoration: none">2016-2019 Copyright by EastoneWashon All rights reserved</a>
            </footer>

            </div>
        </div>
    </div>

    <script type="text/javascript">

        $(document).ready(function () {
            var trigger = $('.hamburger'),
                overlay = $('.overlay'),
                isClosed = true;

            var content = document.getElementById("content-cotainer");
            if(content == null)
                console.log("content:%s", content);

            trigger.click(function () {
                console.log("debug the javascript, click.");
                hamburger_cross();
            });

            $('#wrapper').toggleClass('toggled');

            function hamburger_cross() {

                if (isClosed == true) {
                    console.log("debug the javascript, click, hide()");
                    overlay.show();
                    trigger.removeClass('is-closed');
                    trigger.addClass('is-open');
                    content.setAttribute('class', 'container')
                    isClosed = false;
                } else {
                    console.log("debug the javascript, click, show()");
                    overlay.hide();
                    trigger.removeClass('is-open');
                    trigger.addClass('is-closed');
                    content.setAttribute('class', 'container content-open')
                    isClosed = true;
                }
            }

            $('[data-toggle="offcanvas"]').click(function () {
                $('#wrapper').toggleClass('toggled');
            });

            $(function () {
                $(".dropdown-toggle").dropdown();
            });

            /*$('#id_refresh_switch').bootstrapSwitch({
                onText: "on",
                offText: "off",
                onColor: "success",
                offColor: "info",
                size: "mini",
                onSwitchChange: function(event, state) {
                    if (state == true) {
                        $(this).val("1");
                    } else {
                        $(this).val("0");
                        alert(state);
                    }
                }
            });

            function changeState(stateValue){
                if(stateValue==1){
                    $('#id_refresh_switch').bootstrapSwitch('setState', true, true);
                }else{
                    $('#id_refresh_switch').bootstrapSwitch('setState', false, true);
                }
            }*/

            $('#id_refresh_switch input').bootstrapSwitch();

            $('#id_refresh_switch input').bootstrapSwitch('state',false);

            $('#oper-btn-sw').click(function(){
                $('#id_refresh_switch input').bootstrapSwitch('toggleState');
            });

            var id;

            $('#id_refresh_switch input').on('switchChange.bootstrapSwitch',function(event,state){

                console.log(event);
                console.log(state);

                if(state){
                    id = window.setInterval (function () {
                        window.location.reload();
                    },3000);
                }else{
                    clearInterval(id);
                }
            });
        });

    </script>
</body>
</html>